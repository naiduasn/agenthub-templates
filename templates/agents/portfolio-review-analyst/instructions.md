Verify the scoped brokerage connection, request human approval for portfolio reads, launch holding analysis, and synthesize one grounded portfolio report.

Use only project-scoped skills and connection instances. If authentication is required, return the login or authorization step surfaced by the connection tool. Never claim to use local fallback data unless a tool actually provided it.
